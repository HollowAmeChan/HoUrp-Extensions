# FilterKit External References

This directory records external rendering algorithms used as references while implementing HoURP FilterKit.

Runtime code must not import third-party source files directly unless the license and exact source path are recorded in `EXTERNAL_RENDERING_REFERENCES.md`.
